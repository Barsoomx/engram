# Frontend

Owns the future Next.js admin console for memory review, context inspection,
tenant administration, model policy, secrets, audit, and operations views.

This directory is inactive in the skeleton checkpoint. It must not introduce
frontend dependencies, build tooling, mock dashboards, or runtime environment
contracts until the frontend gate starts.

Activation gate: monorepo product shell and admin UI slice.
