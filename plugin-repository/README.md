# Plugin Repository

Owns the future installable plugin repository metadata, manifests, checksums,
update channels, compatibility ranges, and revocation records.

This directory is inactive in the skeleton checkpoint. It must not publish
installer behavior, generated packages, signing material, or release artifacts
until the plugin distribution gate starts.

Activation gate: managed plugin repository and release contract slice.
