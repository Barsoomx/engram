[tool.poetry]
name = "engram-mcp"
version = "0.1.0"
description = "Thin MCP bridge over Engram server APIs."
authors = ["Engram"]
packages = [{ include = "engram_mcp" }]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
