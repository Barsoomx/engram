# CLI

Owns the future `engram` command-line client for connect, doctor, disconnect,
dry-run, hook bootstrap, and operational checks.

This directory is inactive in the skeleton checkpoint. It must not introduce a
persistent local memory service, local database, provider secrets, or background
summarization behavior.

Activation gate: first hook/client API parity slice.
